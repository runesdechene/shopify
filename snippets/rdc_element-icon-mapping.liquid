{%- comment -%}
  RDC Element Icon Mapping
  
  Ce snippet permet de mapper les éléments de produit vers des icônes personnalisées.
  Les icônes doivent être placées dans le dossier assets avec le format: rdc_icon-[nom].png
  Format recommandé: PNG avec fond transparent, 64x64px minimum
  
  Usage: {% render 'rdc_element-icon-mapping', element_text: element %}
  Output: Le nom du fichier d'icône ou 'default' si aucun mapping n'existe
{%- endcomment -%}

{%- liquid
  assign element_lower = element_text | downcase | strip
  assign icon_name = 'default'
  
  case element_lower
    when '100% coton bio brossé'
      assign icon_name = 'arbre'
    when 'illustration faite à la main'
      assign icon_name = 'no-ia'
    when 'imprimé par nos soins'
      assign icon_name = 'eau'
    when "respect de l'histoire & de l'environnement"
      assign icon_name = 'rune'
    when 'livraison sans plastique'
      assign icon_name = 'colis'
  endcase
  
  echo icon_name
-%}
